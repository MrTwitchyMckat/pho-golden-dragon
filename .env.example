# Nuxt (public — embedded at build time for this static app)
NUXT_PUBLIC_SANITY_PROJECT_ID=03e15w02
NUXT_PUBLIC_SANITY_DATASET=production

# Studio + seed script (optional overrides)
SANITY_STUDIO_PROJECT_ID=03e15w02
SANITY_STUDIO_DATASET=production

# Create at https://www.sanity.io/manage → Project → API → Tokens (Editor)
# Used by: npm run sanity:seed (loads this file from repo root, or studio/.env)
SANITY_API_WRITE_TOKEN=

# Optional Viewer token for local draft preview (`npm run dev`, then open /api/draft/enable).
# If unset, preview uses SANITY_API_WRITE_TOKEN. Never required for the production build.
# SANITY_API_READ_TOKEN=
